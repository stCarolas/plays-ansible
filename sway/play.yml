---
- hosts: localhost
  connection: local
  tasks:
    - include_tasks: "./{{ item }}/install.yml"
      tags: install
      with_items: "{{ sway }}"

    - include_tasks: "./{{ item }}/config.yml"
      tags: config
      with_items: "{{ sway }}"

