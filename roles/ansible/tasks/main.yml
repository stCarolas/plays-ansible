- name: Has alias for ansible-playbook
  tags: ansible
  lineinfile:
    line: "alias ap=ansible-playbook" 
    dest: ~/.config/bash/ansible
    create: yes
    state: present
    regexp: "alias ap=ansible-playbook"
