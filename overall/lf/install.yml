---
- name: Has LF downloaded
  get_url:
    url: "https://github.com/gokcehan/lf/releases/download/nightly/lf-linux-amd64.tar.gz"
    dest: /tmp

- name: Has LF unpacked
  shell: "cd /tmp && tar -xvzf lf-linux-amd64.tar.gz"

- name: Has LF installed
  shell: "cp -f /tmp/lf ~/.local/bin/"
