# Created by newuser for 5.4.1
# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=1000
SAVEHIST=1000
setopt appendhistory autocd nomatch
unsetopt beep extendedglob
bindkey -v
# End of lines configured by zsh-newuser-install
# The following lines were added by compinstall
zstyle :compinstall filename '/home/stcarolas/.zshrc'

autoload -Uz compinit
compinit
zstyle ':completion:*' menu select
zstyle ':completion:*' rehash true
# End of lines added by compinstall

for file in ~/.config/zsh/*; do
  source "$file"
done

autoload -Uz add-zsh-hook
chback_on_chdir () {
  clear
  ls -1 --group-directories-first | m1 --no-input
}
add-zsh-hook chpwd chback_on_chdir

back() { BUFFER="cd .."; zle accept-line; }
zle -N back
bindkey "\eh" back

edit() { BUFFER="fzf --bind \"enter:execute(nvim {})+abort\""; zle accept-line; }
zle -N edit
bindkey "\eo" edit

projects() { BUFFER="projects"; zle accept-line; }
zle -N projects
bindkey "\ep" projects

autoload -z edit-command-line
zle -N edit-command-line
bindkey "\ei" edit-command-line
 
bindkey -s "\el" "ls -1 --group-directories-first\n"
