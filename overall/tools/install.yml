---
- name: Has tools
  become: true
  package:
    name: "{{ item }}"
  with_items:
    - fzf
    - atool
    - the_silver_searcher
    - conky

- name: Has python tools
  become: true
  pip:
    name: "{{ item }}"
  with_items:
    - "ansible-container"
