---
- hosts: localhost
  connection: local
  tasks:
    - include_tasks: "./{{ item }}/install.yml"
      tags: install
      with_items: "{{ devel }}"

    - include_tasks: "./{{ item }}/config.yml"
      tags: config
      with_items: "{{ devel }}"

