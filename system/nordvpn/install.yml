---
- name: Has neccessary packages for nordvpn
  become: true
  package:
      name: "{{ item }}"
  with_items:
    - openvpn

- name: Has folder for openvpn
  become: true
  file:
    path: /etc/openvpn
    state: directory
